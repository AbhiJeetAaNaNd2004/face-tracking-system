### Face Tracking System - Backend Environment File

# Application secret key (used for JWT). Change in production.
SECRET_KEY=replace-this-secret

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=face_tracking
DB_USER=postgres
DB_PASSWORD=password

# Optional frontend base URL used for CORS
FRONTEND_URL=http://localhost:3000